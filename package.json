{
  "private": true,
  "name": "preact_marketplace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "preact"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^7.17.0",
    "eslint-config-preact": "^1.1.3",
    "preact-cli": "^3.0.0",
    "sirv-cli": "^1.0.3"
  },
  "dependencies": {
    "@preact/signals": "^1.0.4",
    "framer-motion": "^7.3.5",
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2",
    "preact-router": "^4.1.0",
    "react-country-flag": "^3.0.2",
    "react-slick": "^0.29.0",
    "sass": "^1.54.9",
    "sass-loader": "^10.2.0",
    "slick-carousel": "^1.8.1"
  }
}
